services:
  postgres:
    image: postgres:17
    container_name: vocabbee-postgres
    restart: unless-stopped

    env_file:
      - ../../.env.docker

    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGSSLMODE: ${PGSSLMODE:-disable}

    ports:
      - "${POSTGRES_PORT_HOST:-5433}:5432"

    volumes:
      - pg_data:/var/lib/postgresql/data

    networks:
      - vocabbee-network

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: vocabbee-backend:latest

    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      tags:
        - vocabbee-backend:latest

    container_name: vocabbee-backend
    restart: unless-stopped

    env_file:
      - ../../.env.docker

    depends_on:
      postgres:
        condition: service_healthy

    ports:
      - "${HTTP_PORT:-8011}:8000"

    volumes:

      - ../../credentials:/project/credentials:ro

    networks:
      - vocabbee-network

    healthcheck:
      test: ["CMD", "python", "-c", "print('healthy')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
    external: true
    name: vocabbee_pg_data

networks:
  vocabbee-network:
    external: true